import { Notyf } from 'notyf';

const notyf = new Notyf({
  ripple: true,
  dismissible: true,
  duration: 5000,
  position: {
    x: 'right',
    y: 'top',
  },
});
// notyf.success('Please fill out the form');
